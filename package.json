{
  "name": "socket-message-server",
  "version": "0.0.1",
  "description": "sending messages from one PC to another",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "panhao wu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "socket.io": "^2.0.3"
  }
}
